import 'package:flutter/material.dart';

class ExampleTab extends StatelessWidget {
  const ExampleTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(20),
      child: const Center(
        child: Text('List of examples'),
      ),
    );
  }
}
